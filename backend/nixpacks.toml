[phases.setup]
nixPkgs = ["chromium", "nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm && pnpm install"]

[phases.build]
cmds = ["pnpm prisma generate && pnpm build"]

[start]
cmd = "pnpm start"
